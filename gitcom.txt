git init

echo >> redme.md

$ git config --local user.name "Dimid"

$ git config --local user.email "d@gmail.com"

git add -A

git status

git commit -a -m "namecommit"

git log --all --graph --pretty=format:'%Cred%h%Creset -%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative